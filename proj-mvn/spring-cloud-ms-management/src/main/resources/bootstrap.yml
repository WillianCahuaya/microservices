spring:
  application:
    name: configuration #and connection
  cloud:
    config:
      uri: http://localhost:8888/ms-configserver
  profiles:
    active: asus

management:
  endpoints:
    web:
      exposure:
        include: "*"