spring:
  application:
    name: configuration #and connection
  cloud:
    config:
      uri: http://localhost:8888/ms-configserver
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"