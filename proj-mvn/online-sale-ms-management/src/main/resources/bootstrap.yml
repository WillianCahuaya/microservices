spring:
  application:
    name: configuration #and connection
  cloud:
    config:
      #uri: http://localhost:8888/ctxpath-ms-configserver
      uri: http://192.168.241.129:8888/ctxpath-ms-configserver
      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20
  profiles:
    active: asus

management:
  endpoints:
    web:
      exposure:
        include: "*"