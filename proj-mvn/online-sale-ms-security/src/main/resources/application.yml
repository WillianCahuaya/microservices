server:
  port: 8086
  servlet:
    context-path: /ctxpath-ms-security

spring:
  application:
    name: appname-ms-security
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/