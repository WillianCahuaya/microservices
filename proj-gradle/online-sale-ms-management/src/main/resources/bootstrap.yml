spring:
  application:
    name: configuration #connection
  cloud:
    config:
      uri: http://localhost:8888/ms-config-server
  profiles:
    active: dev,prd

management:
  endpoints:
    web:
      exposure:
        include: "*"
